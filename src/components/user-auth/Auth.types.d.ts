// User name description, Because the username is used in the registration and authentication components,
// it's better to describe it separately to avoid duplication

export interface AuthUser {
    name: string
}